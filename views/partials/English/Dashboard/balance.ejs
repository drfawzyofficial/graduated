<div class="main-panel">
    <div class="content">
        <div class="container-fluid">
            <h4 class="page-title mb-0">Balance</h4>
            <p>In Balance Section, After you finish the course with users, You can send request to us for sending money
                to your email and we will immediately review your request through one day and send a Bill to your email
                address</p>
            <div class="paymentStatus">
                <div class="start"><span class="badge badge-danger">Start</span> means you didn't send a request </div>
                <div class="pending mt-2"><span class="badge badge-warning">Pending</span> means you sent a request but
                    it's still under review</div>
                <div class="completed mt-2"><span class="badge badge-success">Completed</span> means a bill has been sent
                    to your emill address with Money</div>
            </div>
            <div class="row mt-3">
                <% courses.forEach((course) => { %>
                <div class="col-lg-3 col-md-6 col-12">
                    <div class="card text-center" style="border-radius: 15px">
                        <div class="card-header">
                            <img src="/assets/centerphotos/<%= course.centerphoto %>"
                                style="height: 50px; width: 50px; border-radius: 50%; border: 1px solid #888" />
                            <h4 class="card-title"><%= course.coursename %></h4>
                            <% if(course.paymentStatus === "start") { %>
                                <button class="btn btn-danger w-100">Start</button>
                              <% } else if(course.paymentStatus === "pending") { %>
                                <button class="btn btn-warning w-100">Pending</button>
                              <% } else { %>
                                <button class="btn btn-success w-100">Completed</button>
                              <% } %>
                            <div class="w-100 mt-1 p-2 rounded-lg bg-light border">
                                <h6 class="mb-0">Completed Profit <%= course.courseprice * course.users.length  %>$ </h6>
                            </div>
                            <div class="w-100 mt-1 p-2 rounded-lg bg-light border">
                                <h6 class="mb-0">Recieved <%= (70 / 100) * course.courseprice * course.users.length  %>$ </h6>
                            </div>
                            <div class="w-100 mt-2">
                                <button  type="button" data-courseid="<%= course._id %>" class="makeRequest btn btn-primary w-100" <%= course.users.length === 0 || course.users.length > 0 && moment(course.courseend).valueOf() > Date.now() || course.paymentStatus != "start"? "disabled" : "" %>>Make Request</button>
                            </div>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</div>